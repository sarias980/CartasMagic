<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:defaultValue="0"
        android:entries="@array/pref_color_list_titles"
        android:entryValues="@array/pref_color_list_values"
        android:key="Color"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="Color" />
    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_rareza_list_titles"
        android:entryValues="@array/pref_rareza_list_values"
        android:key="Rareza"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="Rareza" />

</PreferenceScreen>
